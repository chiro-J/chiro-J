class Dog {         // Dog라는 클래스 생성
    bark() {        // bark라는 메서드 생성
        console.log("멍멍");
    }
}

let dog = new Dog();        // Dog 클래스를 사용하는 dog라는 객체 생성
dog.bark();                 // dog 객체에게 bark 메서드 호출시키기

